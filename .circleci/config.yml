version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  test:
    working_directory: $HOME
    executor: python/default
    steps:
      - run:
          command: ls
          name: Test

workflows:
  main:
    jobs:
      - test

